extends ../layout/common

block content
    h1 Update book's title
    form(action="/books/" + book.id + "/update", method="POST")
        label Title
        div
            input(type="text", name="title")
        button Update